package com.omniforge.ai

import io.flutter.embedding.android.FlutterActivity

// FlutterActivity's default configureFlutterEngine() already registers all
// generated plugins via GeneratedPluginRegistrant — no manual override is
// needed with the v2 embedding (declared via the flutterEmbedding=2
// meta-data in AndroidManifest.xml). Add custom platform channels or
// native integrations here as the app grows.
class MainActivity : FlutterActivity()
