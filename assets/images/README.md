# assets/images/

Bundled static images (onboarding illustrations, empty-state graphics,
provider logos, etc.) go here. This directory is currently empty — add
real production artwork before shipping a release build that references
specific image filenames from `lib/`.
